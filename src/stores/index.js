import { createStore } from 'vuex';
import menuTree from './modules/treeMenu';

export default createStore({
   state: () => ({
   }),
   mutations: {
   },
   actions: {
   },


   
   modules: {
      menuTree
   }
});
