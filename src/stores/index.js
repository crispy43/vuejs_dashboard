import { createStore } from 'vuex';
import treeMenu from './modules/treeMenu';

export default createStore({
   state: () => ({
   }),
   mutations: {
   },
   actions: {
   },


   
   modules: {
      treeMenu
   }
});
